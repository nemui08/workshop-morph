# workshop-morph (CLI)

รันวัดผล Opening บน 50 รูป เหรียญ/ทราย

```
python -m pip install -r requirements.txt
python main.py
```

เทอร์มินัลพิมพ์ Confusion Matrix + สูตร
Matplotlib เปิดหน้าต่างกราฟ (และเซฟ `result_plot.png`)
